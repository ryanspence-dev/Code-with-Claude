# Personal Notes

Private notes — not for sharing with a team.

## Version Control

Commit after every iteration:

- **Minor** — small changes, e.g. an extra input check, a tweak to output formatting
- **Major** — a new feature added, e.g. grade boundaries, pass/fail count

Commit message should make it clear which type it is.
